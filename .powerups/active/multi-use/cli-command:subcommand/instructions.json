{
  "name": "cli-command:subcommand",
  "description": "Scaffolds a CLI command that groups multiple subcommands under a shared entry point.",
  "variables": {
    "required": [
      "name",
      "description",
      "sub",
      "subDescription"
    ]
  },
  "intent": [
    "cli command with subcommand",
    "command group",
    "powerups cli"
  ],
  "output": {
    "create": [],
    "modify": [],
    "delete": []
  },
  "includes": [
    {
      "name": "cli-command",
      "variables": {
        "name": "{{name}}",
        "description": "{{description}}"
      }
    },
    {
      "name": "cli-subcommand",
      "variables": {
        "name": "{{name}}",
        "sub": "{{sub}}",
        "subDescription": "{{subDescription}}"
      }
    }
  ]
}