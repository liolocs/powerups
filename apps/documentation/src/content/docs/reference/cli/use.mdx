---
title: 'pup use'
description: 'Use a powerup'
sidebar:
  order: 5
---
import CodeBlock from "@/components/ui/astro/code-block/CodeBlock.astro";

Use a powerup


<CodeBlock autoHeight files={[
  {
    filename: 'bash',
    language: 'bash',
    code: `pup use <powerup-name> [flags]`
  }
]} />

## Flags

| Flag | Short | Type | Description |
| ---- | ----- | ---- | ----------- |
| `--dry-run` | `-dr` | boolean | Print output to stdout instead of writing files |
| `--target-dir` | `-td` | string | Target directory for the use command |

