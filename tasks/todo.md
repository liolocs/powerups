# TODO: Docs Website Autogeneration (CLI + SDK)

Plan: `docs/superpowers/plans/2026-09-03-docs-website-plan.md`

Status: **complete** — all tasks done and verified in the browser.

## Phase 1: CLI reference pipeline

## Task 1: Docs generator script + CLI command pages + site config ✓

**Description:** Created `apps/docs-website/scripts/generate-docs.ts` (bun) that
imports `../../packages/cli/lib/commands/index.js` (guarded), extracts
command metadata with structural types, and renders
`scripts/templates/cli-command.md.njk` into
`src/content/docs/reference/cli/<name>.md` per command. Updated
`astro.config.mjs` (title `powerups`, repo social link, lucode nav links,
sidebar with autogenerated `reference/cli`); deleted the scaffold
`reference/example.md`.

**Acceptance criteria:**
- [x] Generator writes `reference/cli/{build,create,install,uninstall,use}.md` with descriptions and flag tables matching the `Command` objects
- [x] Guard exits with "build the packages first: pnpm --filter @liolocs/powerups-cli build" when the registry is missing
- [x] Generated frontmatter carries `sidebar.order` from registry order; the Starlight sidebar shows the CLI commands in order (verified via chrome-devtools snapshot)
- [x] Stale command pages are removed on regeneration (`rm` + `mkdir` before writing)
- [x] Second run is byte-identical (verified via shasum diff)

**Verification:**
- [x] `bun run scripts/generate-docs.ts` after a CLI build
- [x] `astro dev --background` + chrome-devtools: command pages render under Reference → CLI

**Files touched:**
- `apps/docs-website/scripts/generate-docs.ts` (new)
- `apps/docs-website/scripts/templates/cli-command.md.njk` (new)
- `apps/docs-website/astro.config.mjs` (rewritten)
- `apps/docs-website/src/content/docs/reference/*.md` (generated)
- `apps/docs-website/src/content/docs/reference/example.md` (deleted)

## Task 2: SDK reference page generated from the SDK README ✓

**Acceptance criteria:**
- [x] `reference/sdk.md` renders the full SDK API documentation under Reference → SDK (headings verified: Schemas, Types, defineInstructions, includePowerup + subsections)
- [x] No duplicated H1 (stripped; frontmatter title covers it)
- [x] Second run is byte-identical

**Files touched:**
- `apps/docs-website/scripts/generate-docs.ts` (SDK step)
- `apps/docs-website/scripts/templates/sdk-reference.md.njk` (new)
- `apps/docs-website/src/content/docs/reference/sdk.md` (generated)

## Checkpoint: CLI + SDK reference ✓
- [x] Both reference pipelines generate; rerun is byte-identical
- [x] Pages verified in the browser via chrome-devtools (no console errors)

## Phase 2: Overview + guides

## Task 3: Landing page + guides from templates ✓

**Acceptance criteria:**
- [x] Landing page renders hero + cards linking to install/quick-start/reference (verified via snapshot)
- [x] Guides contain only commands that exist (no `project init`/`add`/`find`)
- [x] All scaffold content replaced; every sidebar/nav link resolves
- [x] Site reads: overview → install → usage → reference

**Files touched:**
- `apps/docs-website/scripts/templates/index.mdx.njk`, `guide-install.md.njk`, `guide-quick-start.md.njk`, `guide-authoring.md.njk` (new)
- `apps/docs-website/scripts/generate-docs.ts` (static pages step)
- `apps/docs-website/src/content/docs/index.mdx`, `guides/{install,quick-start,authoring-powerups}.md` (generated)
- `apps/docs-website/src/content/docs/guides/example.md` (deleted)

## Checkpoint: full docs content ✓
- [x] Overview → install → usage → reference flow complete in the browser

## Phase 3: Integration and verification

## Task 4: Build wiring and end-to-end verification ✓

**Acceptance criteria:**
- [x] `docs` script added; `dev`/`build` chain the generator explicitly (`bun run docs && astro dev` / `bun run docs && astro build` — pnpm does not run `pre`/`post` lifecycle scripts by default, so npm-style `predev`/`prebuild` hooks would never fire)
- [x] `pnpm --filter powerup-agents-docs build` succeeds (generation + astro build; 11 pages)
- [x] `astro dev --background` serves the site; chrome-devtools walkthrough of landing, guides, CLI command pages, SDK reference — sidebar, headings, flags tables render correctly, zero console errors

**Files touched:**
- `apps/docs-website/package.json`

## Task 5: Housekeeping ✓

**Acceptance criteria:**
- [x] Generator rerun is byte-identical across all pages
- [x] `npx eslint apps/docs-website/scripts/generate-docs.ts` — zero problems; `pnpm knip` — zero findings referencing docs-website
- [x] `CONTRIBUTING.md` documents docs regeneration (new "Generated docs website" section)
- [x] `apps/docs-website/AGENTS.md` notes the generator, its sources, and the build prerequisite

## Checkpoint: Complete
- [x] All acceptance criteria met
- [x] Ready for review
