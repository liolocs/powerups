{
  "name": "new-store",
  "variables": ["storeName"],
  "intent": ["create a new store", "svelte writable store"],
  "output": {
    "files": [
      {
        "name": "{{storeName}}.ts",
        "template": "store.ts.tmpl",
        "outputPath": "frontend/src/stores/{{storeName}}.ts"
      }
    ]
  }
}
