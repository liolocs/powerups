{
  "name": "@liolocs/powerups-cli",
  "version": "0.1.0",
  "author": "lioloc@pm.me",
  "description": "A system for AI to use existing code rather than come up with it from scratch",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/liolocs/powerups",
    "directory": "packages/cli"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "npm run clean && tsgo && tsup && cp src/app.tsconfig.json lib && mkdir -p lib/private/scaffold/templates && cp src/private/scaffold/templates/*.njk lib/private/scaffold/templates/ && npm run build:builtins",
    "clean": "rm -rf ./lib",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "prepublishOnly": "npm run build",
    "test": "npx proby",
    "b:link": "bun run scripts/link.ts",
    "build:builtins": "bun run scripts/build-builtin-powerups.ts",
    "release": "commit-and-tag-version --preset conventionalcommits",
    "release:dry-run": "commit-and-tag-version --preset conventionalcommits --dry-run"
  },
  "type": "module",
  "files": [
    "/lib/bin.js",
    "/lib/bin.js.map",
    "/lib/private/scaffold/**/*.njk",
    "/lib/private/builtin/create-powerup/**",
    "!/**/*.spec.*"
  ],
  "imports": {
    "#*": {
      "@powerups/source": "./src/private/*.ts",
      "default": "./lib/private/*.js"
    }
  },
  "dependencies": {
    "@rcompat/cli": "^0.25.0",
    "@rcompat/error": "^0.9.0",
    "@rcompat/fs": "^0.35.1",
    "@rcompat/io": "^0.11.0",
    "@rcompat/is": "^0.12.0",
    "@rcompat/runtime": "^0.17.0",
    "@rcompat/string": "^0.22.0",
    "@rcompat/type": "^0.18.0",
    "nunjucks": "^3.2.4",
    "pema": "^0.8.2",
    "tsup": "^8.5.1"
  },
  "devDependencies": {
    "@liolocs/powerups-sdk": "workspace:^",
    "@liolocs/program": "workspace:^",
    "@types/nunjucks": "^3.2.6",
    "commit-and-tag-version": "^13.1.2"
  },
  "bin": {
    "pup": "./lib/bin.js"
  }
}
