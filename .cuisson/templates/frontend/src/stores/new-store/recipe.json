{
  "name": "new-store",
  "variables": ["storeName"],
  "output": {
    "files": [
      {
        "name": "{{storeName}}.ts",
        "template": "store.ts.tmpl",
        "outputPath": "frontend/src/stores/{{storeName}}.ts"
      }
    ]
  }
}
